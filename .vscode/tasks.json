{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run server",
            "type": "shell",
            "command": "yarn run run",
            "group": "test"
        },
        {
            "label": "bundle js",
            "type": "shell",
            "command": "yarn run bundle",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}